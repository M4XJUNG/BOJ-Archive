def solution(sides):
    # big_num = max(sides)
    # small_num = min(sides)
    # result1 = range(big_num - small_num + 1, big_num + 1)
    # result2 = range(big_num + 1, big_num + small_num)
    # return len(result1) + len(result2)
    return min(sides) * 2 - 1