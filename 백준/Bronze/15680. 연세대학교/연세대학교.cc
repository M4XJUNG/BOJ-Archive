#include <stdio.h>

int main() {
    int n;
    scanf("%d", &n);

    switch(n) {
        case 0: printf("YONSEI"); break;
        case 1: printf("Leading the Way to the Future"); break;
    }
}